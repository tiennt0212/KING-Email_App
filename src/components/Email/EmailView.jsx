import React from "react";
import styled from "styled-components";

const EmailViewStyled = styled.div``;

const EmailView = (props) => {
  return <EmailViewStyled></EmailViewStyled>;
};

EmailView.propTypes = {};

EmailView.defaultProps = {};

export default EmailView;
